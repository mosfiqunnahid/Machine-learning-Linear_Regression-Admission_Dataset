# -*- coding: utf-8 -*-
"""Linear_Regression-Admission_Dataset.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/16HcuilbqY-mb_bP4UfahjpXdMP67URso
"""

import numpy as np
import pandas as pd
import seaborn as sns
from sklearn import metrics
import matplotlib.pyplot as plt
import seaborn as sns; sns.set()
from sklearn.metrics import accuracy_score
from sklearn.metrics import classification_report
from sklearn.linear_model import LinearRegression
from sklearn.model_selection import train_test_split

data=pd.read_csv('/content/Admission_Predict.csv')
data

data=data.drop(columns='Serial No.')
data

g = sns.pairplot(data)
g = g.map(plt.scatter)
g.fig.set_size_inches(50,50)

corr = data.corr()
mask = np.triu(np.ones_like(corr, dtype=np.bool))
f, ax = plt.subplots(figsize=(11, 9))
cmap = sns.diverging_palette(220, 10, as_cmap=True)
sns.heatmap(corr, mask=mask, cmap=cmap, vmax=.3, center=0,
            square=True, linewidths=.5, cbar_kws={"shrink": .5})

data=data.iloc[:,:].values
data

x=data[:,:-1]
y=data[:,-1]

x_train,x_test,y_train,y_test=train_test_split(x,y,test_size=0.2,shuffle=True)

Lr=LinearRegression()
Lr.fit(x_train,y_train)

y_pred=Lr.predict(x_test)

DF=pd.DataFrame({'actual':y_test.flatten(),'predcited':y_pred.flatten()})
DF

plt.scatter(y_pred,y_test)
plt.show()

metrics.r2_score(y_test,y_pred)